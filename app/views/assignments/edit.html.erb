<h1 class="text-center">Editing Assignment</h1>

<%= render 'form', assignment: @assignment %>

<%= link_to 'Show', cohort_assignment_path(@cohort, @assignment), class: "btn btn-secondary btn-sm" %> 

<%= link_to 'Back', cohort_assignments_path(@cohort), class: "btn btn-secondary btn-sm" %>
<br><br><br>